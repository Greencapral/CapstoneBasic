from django.apps import AppConfig

class WebScrapingConfig(AppConfig):
    """Конфигурация приложения Django для модуля веб‑скрапинга.

    Класс определяет конфигурацию приложения ``web_scraping`` в рамках Django‑проекта.
    Наследуется от базового класса ``AppConfig``, который предоставляет инфраструктуру
    для инициализации и настройки приложений Django.

    Используется системой Django для корректного обнаружения, загрузки и настройки
    компонентов приложения (моделей, представлений, миграций, шаблонов и т. д.)
    при старте проекта.

    Атрибуты:
        name (str): Полное Python‑имя приложения. Указывает Django, где находится
            модуль приложения. Должно точно соответствовать имени директории
            с приложением (в данном случае — ``"web_scraping"``).
            Django использует этот атрибут для:
            * автоматического обнаружения приложения при сканировании ``INSTALLED_APPS``;
            * корректной загрузки моделей, миграций и других компонентов;
            * разрешения ссылок на приложение в коде (например, через ``get_app_config()``
              или ``get_model()``).
    """
    name = "web_scraping"
